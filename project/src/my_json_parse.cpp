#include <iostream>
#include <sstream>
#include <string>

int json_parse(const std::string& s) {



//     enum {OUT, IN, USER, EVENT, DATE, 
//           BEGIN, END, GROUP, ERROR} state = OUT;
//     if (state == OUT && *(s+1) && *(s+1) == '{') {
//         state = IN;
//         s = s + 1;
//     } else if (state == IN && strchr(s, ':'))
}